# 2026-02-21 — Saturday

## Key Events

### Mac Power Issue
- Mac shut down ~15:00 due to battery drain (was on battery, not plugged in)
- Restarted ~15:01, back on battery at 100%
- Alex plugged it in ~16:41 → AC Power confirmed
- Created `com.starbox.caffeinate.plist` launchd agent for auto caffeinate on boot
- Added Startup Checks to HEARTBEAT.md (Tailscale, caffeinate, power source)

### Tailscale
- Was stopped after reboot, restarted via `open /Applications/Tailscale.app && Tailscale up`
- Both Macs visible: `100.93.198.127` (macbook-pro-de-lopez), `100.103.138.103` (macbook-m4-pro)

### VDrive Migration to Synology NAS
- 5 files migrated from `/mnt/data/vdrive` → NAS `/starbox_drive/vdrive/`
- `drive.js` updated: NAS = primary storage, local = cache/fallback
- Download verified working through NAS

### Synology Firewall
- Alex configured port 5001 restricted to VPS IP only (`83.228.222.180` + `2001:1600:18:200::25c`)
- Done manually in DSM (API doesn't expose firewall management)

### Snipara Partners Integration — COMPLETE
- Workspace exists: `cmltu09g60001y6qg5s1gkdms`, slug `test-workspace-api`, tier SCALE
- Webhook configured: `https://app.vutler.ai/api/v1/webhooks/snipara` + secret
- Integration key: `int_4bff0c8ebadadd952a0b70bea3ea35393b6b1b8b5ebcd70e3b3295a7967a3063`
- API key for integrator ops: vutler key (`[REDACTED]`) works on `/v1/integrator/*`
- **sniparaWebhook.js** deployed — receives events, logs to audit_logs
- **sniparaIntegrator.js** service — wraps all Partners API calls
- **sniparaAdmin.js** API — admin endpoints for client provisioning
- Test client "Vutler QA" created: `snipara_ic_3f676...` API key auto-generated
- **index.js** now volume-mounted from `/home/ubuntu/vutler/app/custom/index.js`

### Social Media Posts
- X/LinkedIn scripts verified: auth OK, dependencies OK
- x-post-3 scheduled 18:00 CET, li-edu-1 scheduled 18:30 CET
- 19 total cron jobs active (17 social + evening audit + DKIM check removed)

### E2E QA Test Results
- Infrastructure: 6/6 containers healthy ✅
- Pages/Routes: 13/13 returning 200 ✅  
- Auth: Both alopez3006 and testqa login OK ✅
- Agent Runtime: @jarvis responds in ~8s ✅
- rocket.cat: definitively neutralized ✅
- Drive API: Working (was testing wrong route `/drive/list` → correct is `/drive/files`) ✅
- Score: 95% — only onboarding wizard missing for new users

## New Vutler API Endpoints
- `POST /api/v1/webhooks/snipara` — Snipara webhook receiver
- `GET /api/v1/webhooks/snipara/health` — webhook health check
- `GET /api/v1/snipara/admin/workspace` — workspace info
- `GET /api/v1/snipara/admin/clients` — list clients
- `POST /api/v1/snipara/admin/clients` — create client (auto-provisions project + API key)
- `GET /api/v1/snipara/admin/clients/:id` — get client
- `PATCH /api/v1/snipara/admin/clients/:id` — update client
- `DELETE /api/v1/snipara/admin/clients/:id` — delete client
- `POST /api/v1/snipara/admin/clients/:id/api-keys` — create API key

## Snipara Client Bundles (cost to Vutler per client)
| Bundle | Price | Queries/mo | Memories | Swarms | Agents/Swarm | Docs | Storage |
|--------|-------|-----------|----------|--------|-------------|------|---------|
| LITE | $19/mo | 500 | 100 | 1 | 5 | 50 | 100 MB |
| STANDARD | $49/mo | 5,000 | 500 | 5 | 10 | 200 | 1 GB |
| UNLIMITED | $99/mo | Unlimited | Unlimited | Unlimited | 20 | Unlimited | 10 GB |

## Key Decisions
- NAS = primary storage for VDrive (local = cache only)
- Synology port 5001 restricted to VPS IP only
- Snipara integration via Partners API (not MCP) for client provisioning
- Auto-provision Snipara project + API key on client creation
- `index.js` now volume-mounted for easier updates

## Soir (22h)
- **Snipara webhook registered** — `PATCH /api/integrator/workspace` OK, webhook URL + secret configured
- **Snipara client provisioning verified** — 2 clients active (Vutler QA + Test Client)
- **kChat polling stopped** — replaced by Vchat bridge
- **Vchat Bridge deployed** — RC Outgoing WebHook → vutler-api PG inbox → Mac polling (60s)
  - PG table `vchat_inbox`, API endpoints: `/api/v1/vchat/webhook`, `/inbox`, `/ack`
  - Bridge key: `6fde82a4a4c52d11b1c02fe6eda270639280b6d77bf34279`
  - rocket.cat reactivated for webhook integration
  - Tested E2E: message from admin → webhook → PG → Mac poll ✅
- **Vchat = official internal comms** (not kChat, not "Vutler chat" — it's "Vchat")
- **Briefs sent on Vchat**: Oscar (#content) + Philip (#design) for landing page knowledge vs memory block
- **Sprint briefed**: Mike (#engineering) + Philip (#design) for 4 user-facing pages (knowledge, memory, agent settings, usage)
- **Jarvis sync script created** — `scripts/jarvis-sync.sh` (push/pull/test), blocked by Snipara rate limit
- **SNIPARA_API_URL fixed on VPS** — was `/mcp/vutler`, now `/mcp`
