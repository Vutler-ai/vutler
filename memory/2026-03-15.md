# 2026-03-15

## Durable Memory — pre-compaction flush

- Snipara key routing hardened operationally:
  - Project endpoint remains canonical for Vutler operations.
  - Team/integrator keys are scope-specific and no longer used as ambiguous fallbacks.
  - `scripts/snipara` now defaults to `vutler`, auto-loads `.env`, and rejects unknown project fallback routes.

- Legacy `/mcp/vutler` fallback risk was mitigated by explicit endpoint routing and runtime restart to purge legacy runners.

- New Snipara task mutation tools are available and validated:
  - `rlm_task_update` works for mutable fields (title/description/priority).
  - `rlm_task_delete` works and was successfully used to remove duplicate failed marketing redo tasks.

- Local execution lane was validated end-to-end:
  - `*-local` owner strategy confirmed via batch test on `mike-local` (create -> claim -> complete all successful).
  - Operational decision: for local repo/code work, assign/claim with `*-local` agent IDs.

- Hierarchical task tools (`rlm_htask_*`) became visible (17 tools exposed).
  - Partial smoke status:
    - `rlm_htask_policy_get`: OK
    - `rlm_htask_tree`: OK with `root_id`
    - `rlm_htask_block` / `rlm_htask_close`: handler reachable
    - `rlm_htask_create_feature`: initially failing due to handler/mapping constraints, then successful with enriched payload; DX patch announced to support minimal call.
  - Remaining practical note: interface ergonomics and parameter normalization still need stabilization for frictionless agent usage.

- Backlog/WIP state at overnight checkpoint remained not production-ready despite structure improvements:
  - Persistent WIP overflow on key owners (notably jarvis/mike-local/mike/sentinel).
  - Large stale in-progress cluster (>24h) and significant pending/failed backlog remained.
  - Recovery strategy formalized: freeze new starts, enforce WIP cap, triage stale (blocked/split/cancel), close-first on P0/P1, duplicate cleanup.

- Morning 05:00 readiness report was delivered with explicit conclusion:
  - Backlog is structured and executable,
  - but not yet ready for full production cadence until WIP/stale/duplicate reduction is actually executed.

## Durable Memory — 2026-03-15 05:30 CET (post-compaction)

- Pre-compaction memory checkpoint was committed to git:
  - commit `64639cf`
  - message: `docs(memory): checkpoint before compaction (2026-03-15)`
  - file: `memory/2026-03-15.md`

- Alex explicitly requested: “Ok lance exécution agressive” for immediate WIP recovery.
  - An aggressive 2-hour recovery run was launched via subagent (session key: `agent:main:subagent:c633b1af-ea8e-4c8d-9953-a5a36c65dae4`).
  - Expected deliverable: before/after metrics, exact changed IDs, remaining blockers, and clean production-ready morning task list.

