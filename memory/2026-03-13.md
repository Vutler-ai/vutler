# 2026-03-13 — Durable Memory

- Enforced workflow decision: **no Snipara Task ID => no execution**; this became an explicit operating rule for ongoing work.
- Added recurring status automation: 15-minute Snipara tracking updates (job id `0b437169-469c-43fc-85de-428d9477f5f0`) with DONE/IN_PROGRESS/BLOCKED style reporting.
- Runtime model routing change in prod Vutler:
  - Mike set to `openai-codex/gpt-5.3-codex` (Codex Spark implementation lane).
  - Michael set to `openai-codex/gpt-5.3-codex` (Codex code review lane).
  - Review/PR routing keywords mapped to Michael.
- Product/process decision: Sandbox UX is **monitor-only** for clients (execution visibility, not code editing IDE behavior).
- Drive policy clarified:
  - Important product/decision docs must be available in Drive.
  - Team Context must contain only **cross-project** rules; project-specific rules stay in project docs.
- Snipara key routing policy clarified:
  - Project key for project MCP endpoint (`test-workspace-api-vutler`).
  - Team key for team endpoint (`/mcp/team/alopez-nevicom-1769121450132`).
  - Integrator key reserved for provisioning projects + client memories (machine-to-machine flows).
- Snipara tool status (current):
  - `rlm_task_list` and `rlm_task_stats` now available on project MCP and usable for tracking.
  - `rlm_task_events` available but can be sparse.
  - Team shared-context write path still dependent on collection access/write permissions.
- CLI migration work completed for helper scripts (moved to CLI-first wrappers):
  - `scripts/snipara-task.sh`
  - `scripts/snipara-remember.sh`
  - `scripts/snipara-recall.sh`
  - `scripts/snipara-broadcast.sh`
  - with `.env` loading added.
- Compliance artifact created and stored:
  - `projects/vutler/snipara-compliance/SNIPARA-COMPLIANCE-MEMORY.md`
  - copied to Drive path `/data/drive/Workspace/projects/vutler/snipara-compliance/`.
- Operational issue identified and partly fixed: owner non-reaction came from tasks staying `pending` with `owner=null`; explicit `task_claim` by owner is now treated as mandatory after create.
