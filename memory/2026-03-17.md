# Memory Log — 2026-03-17

## Durable decisions (Alex)
- Priorité immédiate: focus mise en prod; les chantiers non bloquants sont planifiés post-prod.
- Standard backlog: à partir de maintenant, tout backlog doit être créé dans Snipara en priorité basse (P2) pour ne rien perdre.
- Standard Drive/provisioning client: UX root direct sur le filesystem client (pas de clic intermédiaire `/<client>`), isolation stricte, et chemins internes `/tenants/...` non exposés.

## Execution outcomes (prod/go-live)
- `mike-local` basculé sur `openai-codex/gpt-5.3-codex-spark` pour accélérer les runs critiques.
- Relance massive des tâches hiérarchiques bloquées: 22/22 relancées avec succès (principalement blocages WIP cap/autonomous sweep, pas des blocages techniques durs).
- Job automatique créé pour sweep blocked toutes les 10 minutes (max 30, relance + réassignation intelligente): cron job id `60d13096-edf7-4754-8643-8ed54e3baab3`.

## Roadmap post-prod ajoutée dans Snipara (priorité basse)
- Skills Catalog Visibility (global/workspace/agent + usage matrix).
- Weekly Validation & Hardening Pack.
- LiveKit Integration (Realtime Voice/Video).
- Pack roadmap complet post-prod (Settings Pro, Integrations stabilization+wiring, Nexus UX consistency, Onboarding personality, Task Router/Swarm sync, Mobile responsiveness full app, Automation priority pack, CLI-Anything evaluation).
- Bug UX ajouté en backlog low-priority: navigation navigateur arrière qui désynchronise le menu/sidebar.

## Drive/API incidents and fixes
- Incident 502 confirmé: `vutler-api` crash (module `../services/drive-index` manquant sur VPS) après patch; corrigé avec fallback safe et service restauré (`/health` 200).
- Sync massif Drive VPS effectué (~605 fichiers) + restart API.
- Correctifs appliqués pour téléchargement/preview (`File not found`) avec lookup par id plus robuste + fallback recherche dans scope tenant.
- Règle fonctionnelle validée avec Alex: l’utilisateur ne doit pas voir `/tenants/*`; l’accès doit rester scoped client uniquement.

## CONTEXT HANDOFF SNAPSHOT — for model switch to Opus 4.6 (2026-03-17 late evening)

### Strategic decisions from Alex (must persist)
- Primary objective today: push toward production, ship progress over perfect process.
- Backlog policy: all non-immediate work must be stored in Snipara with low priority (P2 by default), then promoted when execution starts.
- Drive UX policy (client-safe): user should see client filesystem directly at root, not internal tenant paths; internal `/tenants/...` must stay hidden.
- Runtime policy: RLM runtime mandatory for coding/bugfix/feature-test flows; Python env validation is sufficient (Docker not required).
- Triage/execution policy evolved to delivery-first after throughput stalled at 0/hr.

### Agent and model configuration changes made
- Added overflow workers:
  - `jarvis-cloud-local` (spark lane)
  - `claude-sonnet-local`
  - `claude-opus-local`
- Switched `mike-local` to Anthropic Sonnet lane (`anthropic/claude-sonnet-4-5-20250929`).
- Confirmed model availability set includes Sonnet 4.5 and Opus 4.6 (no “Sonnet 4.6” id in current config).

### Cron orchestration architecture (current)
Three-job structure was set to reduce churn:
1) **Triage Orchestrator (Luna)** — every 5 min, strict real-data plan generation.
2) **Executor** — every 5 min, closure-first, fallback to fixed IDs if triage empty.
3) **Controller KPI** — disabled on WhatsApp at Alex’s request when reports became noisy.

Important adjustments done:
- Disabled redundant proactive status/report jobs that spammed WhatsApp.
- Enforced strict no-synthetic-ID policy for Luna after fake IDs were observed.
- Added hard fallback in executor to avoid empty cycles.

### Throughput issue and root-cause findings
- Major issue was not API token expiration (both OpenAI and Claude health checks succeeded).
- Root bottlenecks:
  - Status churn without closure.
  - Over-constrained evidence gates.
  - WIP/cap parking loops (`AUTO_WIP_CAP`) and dependency waits.
  - Orchestrator occasionally producing empty plans.
- Corrective stance adopted: delivery-first window with `DONE_WITH_FOLLOWUPS` for non-critical residuals.

### Concrete production/ops work executed
- VPS access used directly via SSH and key in workspace secrets.
- Bulk sync performed to production Drive area (hundreds of files copied).
- Multiple direct patches applied/reapplied on VPS drive behavior:
  - hide internal tenant paths in user-facing navigation,
  - direct root client view behavior,
  - improved file lookup/download fallback logic for `File not found` issues.
- 502 incident encountered and fixed (API crash from missing module path on VPS, patched with safe fallback and restart).

### Exoscale backup env setup for Rex
Configured in gateway env:
- `EXOSCALE_S3_ENDPOINT=https://sos-ch-gva-2.exo.io`
- `EXOSCALE_S3_REGION=ch-gva-2`
- `EXOSCALE_S3_ACCESS_KEY=...`
- `EXOSCALE_S3_SECRET_KEY=...` (stored redacted in config output)
- `EXOSCALE_S3_BUCKET=vutler`
- `EXOSCALE_WAL_BUCKET=vaultbrix-wal`
Health-check job logic updated to treat Exoscale as primary backup source (not local mount requirement).

### Snipara task-state checkpoint near handoff
Recent verified snapshot:
- `COMPLETED: 25`
- `IN_PROGRESS: 33`
- `PENDING: 111`
- `BLOCKED: 11`

Critical progress just before this handoff:
- Three previously stuck prod-critical IDs were confirmed moved to `COMPLETED`:
  - `cmmt0bqf60007fhtaypoty4nt`
  - `cmms61nn5000ch1bo99ew6wh3`
  - `cmms61fm40016cwteebqa9efp`

### Immediate next-step recommendation for Opus 4.6 continuity
- Continue manual/semiauto closure drill in batches of 3 strict IDs.
- Keep triage strict real-data only; treat empty triage output as failure state.
- Prioritize moving `COMPLETED` above 30 quickly to keep confidence and momentum.
- Maintain delivery-first until throughput stabilizes, then re-tighten quality gates.

