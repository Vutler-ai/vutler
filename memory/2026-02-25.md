# 2026-02-25 — Daily Log

## Session Recovery (~15:00)
- Mac rebooted 2026-02-24 19:58, OpenClaw gateway **did not restart** despite launchd `ai.openclaw.gateway` with KeepAlive+RunAtLoad
- Gateway was down ~19h (24 fév 18:24 → 25 fév 14:57)
- WhatsApp disconnected at 18:24 after 30min timeout, never reconnected
- Alex logged in manually ~14:57 to restart

## Fixes Applied

### 1. Launchd startup delay
- Created `/Users/lopez/.openclaw/scripts/start-gateway.sh` with 15s sleep + network wait (ping anthropic API)
- Updated `ai.openclaw.gateway.plist`: ProgramArguments → bash wrapper, ThrottleInterval 30s, fixed PATH for x64

### 2. Git repo pushed
- Remote: https://github.com/alopez3006/vutler-platform (private)
- Cleaned: removed MongoDB .wt files, tmp/, reinited repo from scratch (was 40MB → 920K)
- Pushed to `main` branch

### 3. Vutler VPS — MongoDB agents lost
- All RC users (agents) disappeared from MongoDB after reboot, only alex + rocket.cat remained
- PostgreSQL intact: all 11 agents, 2 tasks, 3 calendar events, configs — all safe
- Re-provisioned 11 agents (Mike, Victor, Stephen, Jarvis, Andrea, Philip, Luna, Max, Oscar, Nora, Rex) directly in MongoDB
- Reset alex@vutler.com password to Roxanne1212**#

### 4. Express routing bug fixed
- `/admin/usage` route had unclosed callback — marketplace, activity, onboarding were nested inside it
- Fixed in container `/app/index.js`, restarted vutler-api

### 5. Nginx rewrite — RC pages hidden
- Rewrote `/etc/nginx/sites-enabled/vutler` to block all RC routes (channel, direct, etc.)
- All paths now redirect to `/admin/` (Vutler dashboard)
- RC API + sockjs still accessible for backend auth/chat
- Removed duplicate `vutler-landing` config (was causing conflicts)
- Created `/etc/nginx/sites-enabled/vutler-root` for `vutler.ai` domain (was missing)
- Added `/api/v1/` proxy to RC on vutler.ai (login was 404)

### 6. Cron jobs ran successfully
- Evening strategic audit: score 4/10, momentum 2/10
- 2 tweets posted (@Starboxgroup): dashboard sneak peek + positioning vs ChatGPT Teams
- 1 LinkedIn post: "RAG is not enough" (Snipara RELP)
- Rex health check: 85/100, VPS healthy
- Marcus trading: 3 positions opened (BTC €2k, QQQ €2k, DAX €1.5k), 45% cash

## Open Issues
- Root cause of launchd failure at reboot still unclear (plist was correct)
- MongoDB data loss cause unknown — possible volume corruption during reboot
- Need MongoDB backup strategy (daily dump)

## Sprint 7 — Next.js Migration (16:30-17:00)
- Spawned Mike (scaffold), Philip (components), Luna (landing spec) in parallel
- All 3 delivered: Next.js 16 + Tailwind + shadcn/ui project, 6 UI components, landing spec
- Assembled, built, deployed to VPS as `vutler-frontend` Docker container (port 3002)
- Nginx updated: /dashboard and /agents → Next.js, /admin/* → Express fallback
- Fixed: mock data → real API, John Doe → Alex Lopez, duplicate footer, broken logo
- Added Tools section in sidebar (Email, Tasks, Calendar, Drive)
- Câblé all sidebar links and Quick Actions to working routes
- Login page fixed (logo, font contrast, Google button)
- Landing page (vutler.ai) CSS/assets fixed via nginx rewrite

### Skills installed
- `ui-ux-pro-max` (Philip) — design intelligence
- `get-shit-done` (Mike) — spec-driven dev, context rot prevention

### Backlog planned
- Sprint 8: n8n workflow engine (21 pts)
- Sprint 9: Agent templates marketplace (20+ templates from awesome-llm-apps)
- Rex security upgrade (NVISO cyber-security patterns)
