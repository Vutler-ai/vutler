# 2026-02-26 — Night Work Session

## Completed
- **Gemini provider configured**: Added `google-generative-ai` API, models `gemini-2.5-pro-preview-05-06` + `gemini-2.5-flash-preview-05-20`
- **Mike switched to Gemini 2.5 Pro**: Saves Anthropic tokens (reset demain 18h)
- **Gemini API key saved**: `.secrets/vaultbrix-vutler.md`
- **Vaultbrix service role credentials saved**: user `tenant_vutler_service`, password `REDACTED_DB_PASSWORD` (bypasses RLS)
- **Vaultbrix Phase 1 COMPLETE**:
  - Service role connection verified (pooler 6543, no SSL)
  - All 9 tables confirmed existing (Alex created via dashboard)
  - Full CRUD tested (INSERT, SELECT, DELETE)
  - Created shared pool module: `/home/ubuntu/vutler/app/custom/lib/vaultbrix.js`
  - Updated 5 API files: tasks.js, calendar.js, providers.js, settings.js, dashboard.js → all use Vaultbrix
  - Fixed column name mismatch: `assigned_to` → `assignee` (Vaultbrix schema)
  - API rebuilt and deployed, tasks endpoint verified working
- **Agent list accidentally wiped**: config.patch for agents.list replaces entire list. Restored all 13 agents.

## Issues Found
- **config.patch gotcha**: Patching `agents.list` with a single entry REPLACES the whole list, doesn't merge. Must always include full list.
- **Vaultbrix schema differences**: Tables created by Alex via dashboard have slightly different column names (assignee vs assigned_to, from_addr vs from_address, UUID ids vs SERIAL)
- **Vaultbrix service role**: Can do CRUD but NOT DDL (CREATE TABLE denied)
- **Vaultbrix pooler**: No SSL support (port 6543)

## Pending
- Email API: Still uses MongoDB, needs migration to Vaultbrix (column mapping: from_addr, to_addr, etc.)
- Calendar API: Needs column check (has `all_day`, `color` columns not in original schema)
- Git push Vaultbrix changes
- Cloud agent (Gemini) capability test
