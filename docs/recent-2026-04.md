# Late March 2026 snapshot

Résumé rapide des dix derniers commits pour garder la documentation alignée sur le code (mars 31, 2026).

Update Avril 2026:

- `P0 avatars` est désormais cadrée dans [docs/runbooks/production-patching-plan-2026-04.md](docs/runbooks/production-patching-plan-2026-04.md) avec un objectif simple: bibliothèque locale unique, pas d’upload, pas d’image perso, et alignement création/édition d’agent.
- `P1 dashboard hardening` est maintenant documentée dans [docs/runbooks/production-patching-plan-2026-04.md](docs/runbooks/production-patching-plan-2026-04.md): dashboard scoping par workspace, rejet sans contexte auth, et total tokens aligné sur les sources réelles (`llm_usage_logs`, `usage_logs`, `agent_executions`, puis `credit_transactions` en fallback).

1. `f1612f4` **Add reusable avatar persona library** – Ajout d’un helper `frontend/src/lib/avatar.ts`, du script `lib/avatarPath.js`, et d’une vingtaine de nouvelles icônes `/public/static/avatars/*` pour proposer des personas réutilisables dans les pages agents et tableau de bord.
2. `0ac10e9` **Use template avatars for internal agents** – Le helper avatar favorise désormais les templates internes (`frontend/src/lib/avatar.ts`) et évite les avatars manquants quand on crée un agent à partir d’un template.
3. `afab372` **docs: update release snapshot and ref docs index** – La page `docs/recent-2026-03.md` et les références `docs/README.md` décrivent les derniers chantiers, et les specs Nexus (`docs/specs/*`) ont été ajoutées à l’index de la documentation principale.
4. `7f2107a` **Update docs index for enterprise nexus specs** – `docs/README.md` mentionne explicitement les nouveaux specs enterprise Nexus pour que la navigation pointe sur les bons dossiers.
5. `f9369e3` **Improve drive management UX** – Gros chantier dans `api/drive.js`, `services/s3Storage.js` et la page frontend `frontend/src/app/(app)/drive/page.tsx` : meilleure métadonnées, badges, drag/drop move/rename, et alignement avec les nouveaux endpoints `frontend/src/lib/api/endpoints/drive.ts`.
6. `cb6698e` **Normalize agent avatar paths** – Normalisation des chemins d’avatar partagés dans les API (`api/agents.js`, `api/nexus.js`, `api/onboarding.js`) et les composants frontend pour réutiliser `lib/avatarPath.js` + la migration `scripts/migrations/20260331_normalize_agent_avatars.sql`.
7. `9d26b0a` **feat(frontend): add server-backed auth session cookies** – Routes API `/api/session*` et `/api/admin-session` côté Next, et adaptation des hooks/clients (`frontend/src/lib/api/client.ts`, `frontend/src/hooks/useFeatures.ts`, etc.) pour stocker les cookies serveur.
8. `f84e376` **Fix providers, billing, and settings wiring** – Refonte API `api/providers.js`, `api/billing.js`, `api/settings.js` et ajustements frontend (`billing`, `providers`) pour décrypter les providers stockés et exposer correctement les limites.
9. `b8dcf7b` **feat(frontend): gate app routes with session cookies** – Enforcement des snapshots de fonctionnalités via `frontend/src/lib/auth/route-access.ts`, la page `/upgrade/[feature]`, et `frontend/src/proxy.ts` pour rediriger les routes payantes.
10. `27e8bae` **Fix agents nexus sandbox audit wiring** – Harmonisation des routes et services (`api/agents.js`, `services/sandbox.js`, `services/sandboxWorkspace.js`, `frontend/src/app/(app)/sandbox/page.tsx`) pour que Nexus/Sandbox/agents partagent les mêmes audits et états.
